import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  globalMargin: {
    marginHorizontal: 20,
  },
  pokeballBG: {
    position: 'absolute',
    top: -100,
    right: -100,
    opacity: 0.2,
    height: 300,
    width: 300,
  },
  title: {
    fontSize: 35,
    fontWeight: 'bold',
  },
});
